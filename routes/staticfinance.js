const express = require('express');
const router = express.Router();
const { body, validationResult } = require('express-validator');
const StaticFinance = require('../models/staticfinance');

module.exports = router;